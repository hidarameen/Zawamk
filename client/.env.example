# API base URL. Leave empty for relative URLs (recommended when frontend and backend share domain or via reverse proxy)
# VITE_API_URL=https://music.hidar.eu.cc

# For local development the proxy in vite.config.ts handles /api and /uploads
