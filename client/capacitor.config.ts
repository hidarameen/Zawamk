import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hidar.zawamil',
  appName: 'Zawamil',
  webDir: 'dist',
  server: {
    cleartext: true
  }
};

export default config;
